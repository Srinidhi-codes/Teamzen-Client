export default {
  matcher: [
    "/dashboard/:path*",
    "/attendance/:path*",
    "/employees/:path*",
    "/leaves/:path*",
    "/payroll/:path*",
    "/profile/:path*",
    "/analytics/:path*",
    "/login",
    "/register",
  ],
};
